{
  "name": "@papergiant/feedback-example-vanilla",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Task P10 consumer: proves the framework-free surface (@papergiant/feedback/browser + /server, and every non-React entry point) works with no React installed anywhere in this project. Not published; not part of the package's dependency tree.",
  "scripts": {
    "build:pages": "vite build",
    "start:host": "tsx server.ts",
    "test": "playwright test",
    "test:install": "playwright install chromium webkit firefox",
    "test:no-react": "bash run-no-react-check.sh",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@papergiant/feedback": "file:../.."
  },
  "devDependencies": {
    "@hono/node-server": "^1.19.6",
    "@playwright/test": "^1.56.1",
    "@types/node": "^20.19.19",
    "hono": "^4.9.11",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vite": "^6.3.6"
  }
}
