/**
 * @papergiant/feedback — the root entry re-exports the core schema and
 * helpers so a host can `import { SCHEMA } from '@papergiant/feedback'`
 * without reaching into `@papergiant/feedback/core` directly.
 */
export * from './core/index.js';
